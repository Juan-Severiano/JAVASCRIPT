const cartas = [1, 1, 2, 2, 3, 3, 4, 4];

async function imageParGenerate() {
  const parImage = {}
  for (let i=0; i < cartas.length; i++) {
    if (!parImage[cartas[i]]) {
      const id = 0;
    }
  }
}
